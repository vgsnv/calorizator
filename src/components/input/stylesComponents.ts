import palette from "../../constants/palette";

import styled from "styled-components/native";

export const Input = styled.TextInput`
  margin: 15px;
  padding: 16px 8px;
  border-radius: 8px;
  border-color: ${palette.mainDark};
  border-width: 1;
`;
